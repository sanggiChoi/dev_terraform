export TF_PLUGIN_CACHE_DIR="/root/terraform/plugin-cache"
tfenv use 1.0.11 && tgswitch 0.35.10

aws sso login --profile sso-dev
#yawsso -p sso-data
export AWS_PROFILE=sso-dev

aws sts get-caller-identity